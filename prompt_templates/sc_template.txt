给定以下文本和从中提取的关系三元组：

文本: {input_text}
三元组: {query_triplet}

三元组中的关系"{query_relation}"被定义为"{query_relation_definition}"。请从候选中选择与文本事实、关系方向及主体和客体类别最相符的标准关系。

关系名称不同不代表含义不同；候选关系若是覆盖原关系含义的通用术语，可以选择。优先匹配核心谓词、关系方向和语义类别，不要只按词面或数值相似度判断。只有所有候选都表示不同关系、角色相反或语义类别不兼容时，才选择“None of the above”。请仅输出一个选项字母。

选项：
{choices}
