你将获得一段文本和一系列关系三元组，格式为[主体，关系，客体]。请为每种关系撰写可复用的通用语义定义。

定义应使用稳定、通用的领域术语，明确说明主体类别、客体类别、主体到客体的关系方向、核心谓词语义，以及关系适用范围和边界。请将同一关系对应的全部三元组和原文作为实例证据，归纳其中稳定不变的角色类型与关系含义，再概括成适用于该关系的通用定义。根据三元组第一项判断主体类别，根据第三项判断客体类别；不得按文本主题统一假定实体类别，不得交换两端角色，也不要只重复关系名称。区分实体、属性、参数、过程和要求等语义类别。不要将任何实例中的实体名称、具体数值、单位或特定工艺情况写入定义，也不要添加例句。信息不足时使用可由实例支持的上位类别，不推测未提供的类型。

每个关系严格输出一行，字段名称必须准确使用“主体类别”“方向”“客体类别”“谓词语义”“范围边界”，格式为：关系名: 主体类别=...；方向=主体→客体；客体类别=...；谓词语义=...；范围边界=...。主体类别和客体类别必须是抽象类别词，谓词语义必须概括该关系的作用或状态。只输出关系定义，不要输出分析过程。

文本：{text}
三元组：{triples}
关系：{relations}
